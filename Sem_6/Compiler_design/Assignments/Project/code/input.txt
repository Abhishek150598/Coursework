CREATE TABLE STUDENT
(
	NAME VARCHAR(30),
	DEPT VARCHAR(25),
	ROLL NUMBER(20),
	PRIMARY KEY(ROLL)
);

INSERT INTO STUDENT VALUES("ABHIK", "CSE", 224);

INSERT INTO STUDENT(NAME, ROLL) VALUES ("SUPRIYA", 24);

SELECT * FROM STUDENT WHERE ROLL > 30;

SELECT NAME, ROLL FROM STUDENT WHERE ROLL BETWEEN 50;

INSERT INTO STUDENT(NAME, ROLL) VALUES ("ROHAN", "CIVIL", 24);

CREATE TABLE 453
(
	NAME VARCHAR(30),
	DEPT VARCHAR(25),
	ROLL NUMBER(20),
	PRIMARY KEY(ROLL)
);

